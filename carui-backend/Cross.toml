[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update",
    "apt-get install --assume-yes --no-install-recommends clang libclang-dev libv4l-dev:$CROSS_DEB_ARCH libssl-dev:$CROSS_DEB_ARCH libudev-dev:$CROSS_DEB_ARCH",
]